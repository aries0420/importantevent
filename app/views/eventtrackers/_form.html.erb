<%= form_for(@eventtracker) do |f| %>
  <% if @eventtracker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eventtracker.errors.count, "error") %> prohibited this eventtracker from being saved:</h2>

      <ul>
      <% @eventtracker.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
